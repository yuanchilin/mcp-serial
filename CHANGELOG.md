# 更新记录

> 本仓库从 2.6.0 起维护更新记录。更早的版本（2.1.x ~ 2.4.x）见 `git log` 与 git tag；
> npm 上的 `2.5.0` 未在本仓库留下对应提交或 tag（来源待查），故此处不追溯其内容。

## 2.6.0

**多串口并存（一个进程多路，互不干扰）**
- 每路独立持有：串口、环形缓冲区、读游标、控制端、可见性、客户端集合与统计
- 寻址规则：数据类操作必须显式 `port`；省略时只有"恰好一路已打开"才放行，多路直接报错并列出候选（绝不静默发错口）
- `serial_stop` 必须指定 `port`，要全关需显式 `all: true`；`SERIAL_PORTS="COM3@115200,COM5@9600"` 可一次开多路

**端口可见性（仅本机可见）**
- 本机且持有该端口控制权时可将端口设为私有：远程侧**连存在性都不暴露**（列表 / 状态 / 订阅 / 操作一律 404）
- 按端口落盘（默认 `%APPDATA%\mcp-serial\private-ports.json`，可用 `SERIAL_PRIVATE_FILE` 覆盖）

**Web 界面**
- 只保留一个"操作端口"，用侧栏**端口下拉即切换**：不重连、不断开别的路，也不会替用户打开没在跑的端口（下拉里 `●` 标出正在运行的端口）
- 发送前**固件体检**：识别 Intel HEX / Motorola SREC / 裸二进制，给出数据量、记录条数、地址范围与段/空洞、逐行校验、入口地址（HEX 03/05、SREC S7/S8/S9）；有校验或格式错误时发送前必须确认一次
- 按钮按状态置灰：发送类控件（含分块 / 块间延时）随角色与内容置灰，「复制 / 保存日志」在无内容时置灰，「载入历史缓冲」在未连接时置灰
- 「清屏」是纯窗口行为：终端默认**不再自动回放**历史（新窗口 / 刷新后从空开始），要看服务端缓冲历史点「载入历史缓冲」（WS `&replay=1`）
- 控制权申请 / 强制接管 / 应答均按端口寻址（多路下省略端口会被服务端拒绝）

**依赖与工程**
- MCP SDK 升到 v2（`@modelcontextprotocol/server` / `@modelcontextprotocol/node` 2.0.0 + zod 4），远程传输改为 Streamable HTTP（`/mcp`），**运行要求 Node ≥ 20**
- `engines.node` 由 `>=18` 修正为 `>=20`；提交 `package-lock.json`，CI 使用 `npm ci`
- CI：Node 20/22/24 矩阵 + Windows 一份，跑单元测试、真实启动自检（断言横幅版本号 == package.json）与端到端 UI（chromium）
- CD：打 `v*` tag 即发布到 npm（tag 与版本必须一致、版本未被占用、全部检查绿），并自动创建 GitHub Release
